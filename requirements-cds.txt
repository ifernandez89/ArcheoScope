# Copernicus CDS Integration Requirements for ArcheoScope
# Install with: pip install -r requirements-cds.txt

# Core CDS API client
cdsapi>=0.7.7

# Enhanced datastores client (optional, for advanced features)
ecmwf-datastores-client>=0.4.0

# Data processing dependencies
numpy>=1.21.0
pandas>=1.3.0

# Environment and configuration
python-dotenv>=0.19.0

# HTTP requests and networking
requests>=2.25.0
urllib3>=1.26.0

# Progress bars for data downloads
tqdm>=4.62.0

# Date and time handling
python-dateutil>=2.8.0
pytz>=2021.1

# JSON and data serialization
attrs>=21.0.0

# Optional: NetCDF file handling (for ERA5 data)
netcdf4>=1.5.7
xarray>=0.19.0

# Optional: Geospatial data processing
rasterio>=1.2.0
geopandas>=0.10.0

# Optional: Scientific computing
scipy>=1.7.0
matplotlib>=3.4.0